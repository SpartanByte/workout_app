require "rails_helper"

RSpec.feature "Deleting Exercises" do
    # before do
    #     @owner = User.create(first_name: "John", last_name: "Doe", email: "john@example.com", password: "password")
        
    #     @owner_exercise = @john.exercises.create(duration_in_min: 20, workout: "My body building activity",
    #         workout_date: Date.today)

    #     login_as(@john)
    # end
    
    
    # scenario "successfully deleting exercises" do
    #     visit '/'

    #     click_link "My Lounge"

    #     path = "/users/#{@owner.id}/exercises/#{@owner_exercise.id}"
    #     link = "//a[contains(@href,\'#{path}\') and .//text()='Destroy']"

    #     find(:xpath, link).click

    #     expect(page).to have_content("Exercise has been deleted")

    # end
end